using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Projectile_C003_16_Boss : MonoBehaviour
{
    // Start is called before the first frame update
    public void DestroyMe()
    {
        Destroy(gameObject);
    }
}
